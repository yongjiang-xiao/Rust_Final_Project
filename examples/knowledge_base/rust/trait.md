# Rust Trait

A trait defines shared behavior in Rust. Types implement traits to provide
methods that can be called through a common interface. Traits are useful for
polymorphism and generic programming.

For example, a search system can define a Tokenizer trait or a Ranker trait.
Different implementations can follow the same interface while using different
internal algorithms.
