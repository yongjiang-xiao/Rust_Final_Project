# Greedy Algorithm

A greedy algorithm makes the locally best choice at each step. It is simple and
efficient when the problem has a valid greedy property.

Classic examples include interval scheduling and minimum spanning tree
algorithms. Greedy methods are not always correct, so the proof of correctness
is important.
